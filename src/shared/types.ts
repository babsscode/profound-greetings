export interface CardData {
    cardType: string;
    name: string;
    message: string;
    quote: string;
}

export interface CardReturnData {
    cardType: string;
    name: string;
    message: string;
    quote: string;
    _id: string;
}